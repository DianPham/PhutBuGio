@model IEnumerable<Niveau.Areas.Admin.Models.Products.Category>

@{
    ViewData["Title"] = "Index";
}

<h1>Category</h1>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        <a href="@Url.Action("Create")" class="btn btn-primary m-2">Create New</a>
        <a asp-controller="Products" asp-action="List" class="btn btn-primary m-2">Product</a>
        @foreach (var category in Model)
        {
            <tr>
                <td>@category.Id</td>
                <td>@category.Name</td>
                <td>

                    <a asp-action="Details" asp-route-id="@category.Id">View</a> |

                    <a asp-action="Edit" asp-route-id="@category.Id">Edit</a> |

                    <a asp-action="Delete" asp-route-id="@category.Id">Delete</a>

                </td>
            </tr>
        }
    </tbody>
</table>
