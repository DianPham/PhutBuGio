@model Niveau.Areas.Admin.Models.Products.ProductListViewModel

@if (Model.Products.Any())
{
    @foreach (var product in Model.Products)
    {
        <div class="col-lg-4 col-md-6 col-sm-12 pb-1">
            <div class="card product-item border-0 mb-4">
                <div class="card-header product-img position-relative overflow-hidden bg-transparent border p-0">
                    <a asp-controller="Products" asp-action="User_Details" asp-route-id="@product.Id" asp-route-title="@product.Name"><img class="img-fluid w-100" src=@product.ImageUrl alt=""></a>
                </div>
                <div class="card-body border-left border-right text-center p-0 pt-4 pb-3">
                    <h6 class="text-truncate mb-3">@product.Name</h6>
                    <div class="d-flex justify-content-center">
                        <h6>$@product.Price</h6><h6 class="text-muted ml-2"><del>$@(product.Price + product.Price / 5)</del></h6>
                    </div>
                </div>
                <div class="card-footer d-flex justify-content-between bg-light border">
                    <a asp-controller="Products" asp-action="User_Details" asp-route-id="@product.Id" asp-route-title="@product.Name" class="btn btn-sm text-dark p-0"><i class="fas fa-eye text-primary mr-1"></i>View Detail</a>
                    <a asp-area="User" asp-controller="ShoppingCart" asp-action="AddToCart" asp-route-productId="@product.Id" asp-route-quantity="1" class="btn btn-sm text-dark p-0"><i class="fas fa-shopping-cart text-primary mr-1"></i>Add To Cart</a>
                </div>
            </div>
        </div>
    }
}
else
{
    <p>No products available.</p>
}